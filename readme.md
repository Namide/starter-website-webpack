# Website starter (Webpack)

> Simple starter to build a static website with Webpack

## Build Setup

``` bash
# install dependencies
npm i

# serve at localhost:3000
npm run dev

# build for production with minification
npm run build
```